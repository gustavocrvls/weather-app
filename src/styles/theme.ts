import { DefaultTheme } from 'styled-components'

const light: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    primary: 'cyan',
    secondary: 'magenta',
  },
}

const dark: DefaultTheme = {
  borderRadius: '5px',

  colors: {
    primary: 'cyan',
    secondary: 'magenta',
  },
}

export { light, dark }
